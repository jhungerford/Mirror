weather:
  # Register an account at https://developer.forecast.io/, and set the MIRROR_FORECASTIO_API_KEY environment variable
  forecastioApiKey: ${MIRROR_FORECASTIO_API_KEY}
  latitude: ${MIRROR_LATITUDE}
  longitude: ${MIRROR_LONGITUDE}

radar:
  # Find your radar site id at the bottom of this page: http://www.srh.noaa.gov/jetstream/doppler/ridge_download.html
  radarSiteId: ${MIRROR_RADAR_SITE_ID}
